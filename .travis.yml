language: python
python:
 - "3.6"
 - "3.7"
 - "3.8"
 - "3.8-dev"
 - "nightly"
cache: pip
install:
    - pip install -r requirements.txt
    - pip install .
    - pip install smof
script: ./runtests.py && pip install . && cd test-data && ./test.sh && cd clades && ./test.sh
